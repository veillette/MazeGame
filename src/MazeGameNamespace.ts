/**
 * MazeGameNamespace.ts
 *
 * The SceneryStack Namespace for this simulation. Used as the first argument
 * to ProfileColorProperty so color names are scoped to this sim.
 */
import { Namespace } from "scenerystack/phet-core";

const MazeGameNamespace = new Namespace("maze-game");

export default MazeGameNamespace;
